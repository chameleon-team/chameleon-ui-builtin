<script cml-type="interface">
type EventDetail = {
  current: Number
}
interface CCarouselInterface {
  autoplay: Boolean,
  interval: Number,
  circular: Boolean,
  current: Number,
  indicatorDots: Boolean,
  indicatorColor: String,
  indicatorActiveColor: String,
  dotStyle: String,
  dotActiveStyle:String,
  dotsToBottom:Number,
  threshold:Number,
  change(eventDetail: EventDetail): void;
}
</script>
